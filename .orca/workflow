# Orca 多 Agent 协作工作流

核心要点: 每个能力域都会有一个agent负责，一个agent有一个工作树，每个能力域只指定一个推荐 Agent，不重复分配。
评审agent可以读取访问需要评审的工作树
注意!!!(各agent之间尽量不要直接相互交流，有必要的话可以通过项目文件夹里的.orca/talking.txt(每个工作树都有)间接留言)
让每个agent行动前都读取这个文档.orca/workflow,找到他自己负责的内容，不要越界

## 0. 留言板机制（talking.txt）

- **每个工作树都有 `.orca/talking.txt`**（含主项目根目录，各 Agent 认领/汇报时读取自己工作树里的那一份）。
- 留言格式：`[发信人 -> 收信人] 时间/任务: 内容`。只追加，不删除他人留言。
- **Claude（协调者）**：在 `.orca/talking.txt` 分派任务（`[Claude -> <Agent>] 任务号/域: ...`）。
- **各 Agent**：从 `.orca/talking.txt` 接受任务（回 `[<Agent> -> Claude] accept: ...`），完成后回 `[<Agent> -> Claude] complete: ...` 汇报，评审 Agent 据此读取对应工作树评审。
- **任务流转**：分派 → 接受 → 执行（TDD）→ 完成汇报 → 评审 → 合并 main。

### 执行约定

1. **分派**：Claude 仅向注册表中该能力域的推荐 Agent 分派，使用稳定任务号（例如 `S-N1`）；同一任务不得同时分派给多个推荐 Agent。
2. **接收**：接收者行动前先阅读 `.orca/workflow` 和本工作树 `.orca/talking.txt`；收到自己的 `assign` 留言后，先追加 `accept`，再开始工作。未明确分派或不属于本能力域的事项不得认领。
3. **状态**：`complete` 必须包含提交号、验证结果和待评审事项；受阻时追加 `blocked`，说明阻塞原因和所需决定。所有留言只追加，保留原始时间线。
4. **评审**：完成留言应指向工作树与评审 Agent；评审 Agent 可只读访问该工作树，并追加 `review: approved` 或 `review: changes_requested`。未获批准不得合并。
5. **同步**：`main` 的 `.orca/workflow` 与 `.orca/agent-registry.md` 是规则权威副本。每次开始工作及完成工作后，检查本工作树的 `.orca/` 文档是否落后 main；发生冲突时保留双方 `talking.txt` 留言并解决注册表差异。

## 1. Agent 注册表

每个能力域只指定一个推荐 Agent，不重复分配；未覆盖能力保持空白，后续按需补充(不要随意更改由我决定)。
**注册表文件：`.orca/agent-registry.md`**（本文件下方表格为内嵌副本，两者保持一致）：

| 能力域 | 推荐 Agent | 评审 Agent |
|---|---|---|
| 架构 / 代码质量 / 逻辑 / 测试 | Claude | Codex |
| 依赖 / 配置 / 文档 | cline | Claude |
| 安全 / 合规 / 风险 | Codex | cline |
| 性能 | Pi | cline |
| 数据 / 数据库 | opencode | Codex |
| 接口 / 兼容性 | kilo | Claude |
| 前端 / 体验 / 发布 / 运维 | Claude | codex |

## 2. 状态检查

每次完成一个工作都要检查工作树和 `orca\` 里文档的内容，汇报当前状态。
**检查所有工作树的 `.orca/talking.txt`**，合并各 Agent 的接受/完成/问题留言，向用户汇总当前进度与待办。

## 3. git 提交配置

用户的 Git 身份：
- Gitee 账号: ZX666X
- GitHub 账号: ZX466
- 统一邮箱: zx19836980213@outlook.com（已绑定 Gitee 与 GitHub 两账号，故一条提交可同时归属两个平台）
- 提交姓名: ZX666X

机器全局 git 身份已配置：`git config --global user.name=ZX666X / user.email=zx19836980213@outlook.com`；新 clone 仓库无需再单独配置。

## 4. 合并与推送

最后合并成一个 main 分支推送到两个远程仓库——一一照应（origin=Gitee ZX666X，github=GitHub ZX466）。
