# Orca 多 Agent 协作工作流

**核心要点（所有 Agent 行动前必读）：**
- 每个能力域有且只有一个推荐 Agent 负责，一个 Agent 对应一个工作树，不重复分配。
- 评审 Agent 只读取被评审工作树，不修改代码，不越界。
- ⚠️ 各 Agent 之间严禁直接交流；有必要时，通过本工作树 `.orca/talking.txt` 间接留言。
- ⚠️ 任何 Agent 行动前必须先读取本工作树的 `.orca/workflow`，找到自己负责的能力域，只做自己域内的事，不越界。

## 0. 留言板机制（talking.txt）

- **每个工作树都有 `.orca/talking.txt`**（含主项目根目录；各 Agent 认领/汇报时读取**自己工作树**里的那一份）。
- 留言格式：`[发信人 -> 收信人] 时间/任务: 内容`。**只追加，永远不删除他人留言**。
- **Claude（协调者）分派任务**：Claude 在各工作树 `.orca/talking.txt` 写入 `[Claude -> <Agent>] 任务号: 任务内容`；各 Agent 读取本工作树 talking.txt，看到发给自己的 `[Claude -> <Agent>]` 消息即为任务入口，须回 `accept` 认领。
- **各 Agent 任务流转**：
  1. `[Claude -> Agent] assign:` — 任务分派
  2. `[Agent -> Claude] accept:` — 认领（说明工作树、开始时间）
  3. `[Agent -> Claude] progress:` / `blocked:` — 执行中/阻塞（可选）
  4. `[Agent -> Claude] complete: <hash>` — 完成（必须带提交哈希、测试结果）
  5. `[ReviewAgent -> Claude] review: pass | changes_requested` — 评审结果
- **评审流程**：推荐 Agent 完成后回 `complete`；评审 Agent 读取被评审工作树后，回 `review: pass` 或 `review: changes_requested <说明>`。
- **冲突处理**：同一任务只允许一个 Agent 认领；冲突或重复实现标记 `blocked`，等待 Claude 裁决。

## 1. Agent 注册表

每个能力域只指定一个推荐 Agent，不重复分配；未覆盖能力保持空白，后续按需补充(不要随意更改由我决定)。
**注册表文件：`.orca/agent-registry.md`**（本文件下方表格为内嵌副本，两者保持一致）：

| 能力域 | 推荐 Agent | 评审 Agent |
|---|---|---|
| 架构 / 代码质量 / 逻辑 / 测试 | Claude | Codex |
| 依赖 / 配置 / 文档 | cline | Claude |
| 安全 / 合规 / 风险 | Codex | cline |
| 性能 | Pi | cline |
| 数据 / 数据库 | opencode | Codex |
| 接口 / 兼容性 | kilo | Claude |
| 前端 / 体验 / 发布 / 运维 | Claude | codex |

## 2. 状态检查

每次完成一个工作都要检查工作树和 `orca\` 里文档的内容，汇报当前状态。
**检查所有工作树的 `.orca/talking.txt`**，合并各 Agent 的接受/完成/问题留言，向用户汇总当前进度与待办。

## 3. git 提交配置

用户的 Git 身份：
- Gitee 账号: ZX666X
- GitHub 账号: ZX466
- 统一邮箱: zx19836980213@outlook.com（已绑定 Gitee 与 GitHub 两账号，故一条提交可同时归属两个平台）
- 提交姓名: ZX666X

机器全局 git 身份已配置：`git config --global user.name=ZX666X / user.email=zx19836980213@outlook.com`；新 clone 仓库无需再单独配置。

## 4. 合并与推送

最后合并成一个 main 分支推送到两个远程仓库——一一照应（origin=Gitee ZX666X，github=GitHub ZX466）。
